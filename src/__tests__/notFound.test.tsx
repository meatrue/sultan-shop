import React from 'react';
import { render } from '@testing-library/react';
import NotFound from '../pages/not-found/not-found';

describe('Рендер страницы Not Found', () => {
  render(<NotFound />);
});
